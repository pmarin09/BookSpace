<!-- Main jumbotron for a primary marketing message or call to action -->
<main role="main">
<div class="jumbotron">
  <div class="d-flex justify-content-center">
    <div><h1 class="display-3">Welcome to BookSpace</h1>
    <p> Book a desk, open space or meetings rooms through our portal.</p>
    <p><a class="btn btn-primary btn-lg" href="#" role="button"> View Spaces»</a></p>
    </div>
  </div>
</div>

<div class="d-flex justify-content-center">
<div class="row">
<p><% @coworkingspaces.each do |space| %> </p>
  <div class="col-md-4">
    <div class="card-deck">
      <div class="card">
        <%= image_tag space.pic, class: "card-img-top"%>
        <div class="card-body">
          <h5 class="card-title"><%= space.name %></h5>
          <p class="card-text"><%= space.address %></p>
          <p class="card-text"><%= space.spaces_qty %> spaces</p>
          <%= button_to "View", "/coworkingspaces/#{space.id}", :method => "get", class: "btn btn-primary" %>
        </div>
      </div>
    </div>
    <p></p>
    </div>
  <%end%>
  </div>
  </div>

</main>
